<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white"
    android:orientation="vertical"
    android:padding="20dip" >

    <TextView
        android:id="@+id/notes"
        android:layout_width="fill_parent"
        android:layout_height="135dp"
        android:layout_marginBottom="20dp"
        android:gravity="center"
        android:text="Please speak clearly when recording. You can also format your work through instructions like, &apos;New paragraph&apos;, &apos;Heading is&apos;. Maximum file size per recording should not exceed 10MB."
        android:textColor="#FFFFFF"
        android:textSize="15sp" />

    <ToggleButton
        android:id="@+id/tglPlayPause"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:background="@drawable/circle_toggle_btn"
        android:gravity="center_vertical|center_horizontal"
        android:textColor="#fff"
        android:textOff=""
        android:textOn="" />

    <TextView
        android:id="@+id/timerValue"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="37dp"
        android:gravity="center"
        android:text="timerVal"
        android:textColor="#FFFFFF"
        android:textSize="30sp" />

    <Button
        android:id="@+id/reset"
        android:layout_width="86dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:background="@drawable/button_white_control"
        android:gravity="center" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="34dp"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/btnFormat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="0.98"
            android:text="Format (mp4)"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>